{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Upload (Arduino)",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/${file}",
            "cwd": "${workspaceFolder}"
        },
        {
            "name": "Monitor (Arduino)",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/${file}",
            "cwd": "${workspaceFolder}"
        }
    ]
}
